---
interface Props {
	title: string;
}

const { title } = Astro.props;
---

<h2
	slot="title"
	class="mb-8 bg-primary bg-opacity-30 p-4 text-4xl font-semibold text-slate lg:mb-16 lg:text-5xl dark:bg-opacity-50 dark:text-white"
>
	{title}
</h2>
